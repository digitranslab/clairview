apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clairviewcrm-server-pvc
  namespace: clairviewcrm
spec:
  storageClassName: default
  volumeName: clairviewcrm-server-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
