apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: clairviewcrm-server
  name: clairviewcrm-server
  namespace: clairviewcrm
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      app: clairviewcrm-server
  template:
    metadata:
      labels:
        app: clairviewcrm-server
    spec:
      volumes:
        - name: clairviewcrm-server-data
          persistentVolumeClaim:
            claimName: clairviewcrm-server-pvc
        - name: clairviewcrm-docker-data
          persistentVolumeClaim:
            claimName: clairviewcrm-docker-data-pvc
      containers:
        - name: clairviewcrm
          image: clairviewcrm/clairview:latest
          imagePullPolicy: Always
          env:
            - name: PORT
              value: 3000
            - name: SERVER_URL
              value: "https://crm.example.com:443"
            - name: "PG_DATABASE_URL"
              value: "postgres://postgres:postgres@clairviewcrm-db.clairviewcrm.svc.cluster.local/default"
            - name: "REDIS_URL"
              value: "redis://clairviewcrm-redis.clairviewcrm.svc.cluster.local:6379"
            - name: SIGN_IN_PREFILLED
              value: "false"
            - name: STORAGE_TYPE
              value: "local"
            - name: "MESSAGE_QUEUE_TYPE"
              value: "bull-mq"
            - name: "ACCESS_TOKEN_EXPIRES_IN"
              value: "7d"
            - name: "LOGIN_TOKEN_EXPIRES_IN"
              value: "1h"
            - name: APP_SECRET
              valueFrom:
                secretKeyRef:
                  name: tokens
                  key: accessToken
          ports:
            - containerPort: 3000
              name: http-tcp
              protocol: TCP
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "1024Mi"
              cpu: "1000m"
          stdin: true
          tty: true
          volumeMounts:
            - mountPath: /app/docker-data
              name: clairviewcrm-docker-data
            - mountPath: /app/packages/clairview-server/.local-storage
              name: clairviewcrm-server-data
      dnsPolicy: ClusterFirst
      restartPolicy: Always
